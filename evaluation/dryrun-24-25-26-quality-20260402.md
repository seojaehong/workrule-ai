# Dry Run Quality Comparison

검증 일시: 2026-04-02

## Scope

- `24/26`: `취업규칙 24년.pdf` vs `표준취업규칙(2026년, 배포).pdf`
- `25/26`: `(주)내산_2022 취업규칙)개정안_250701.hwp` vs `표준취업규칙(2026년, 배포).pdf`
- 각 조합별 3회 드라이런
- 실행 경로: `FastAPI /api/v1/ingestion/extract-text` -> `FastAPI /api/v1/reviews/diagnose`

## Summary

- `24/26`
  - 회사 문서 파서: `pdf_ocr_fallback`
  - 기준 문서 파서: `pdf`
  - 3회 모두 추출 성공
  - 3회 모두 비교 API 성공
  - OCR 추출 해시 변동 없음
  - 기준 문서 추출 해시 변동 없음
  - 비교 결과 finding 개수 변동 없음

- `25/26`
  - 회사 문서 파서: `hwp`
  - 기준 문서 파서: `pdf`
  - 3회 모두 추출 성공
  - 3회 모두 비교 API 성공
  - HWP 추출 해시 변동 없음
  - 기준 문서 추출 해시 변동 없음
  - 비교 결과 finding 개수 변동 없음

## Interpretation

- `24/26` 조합은 업스테이지 `Document Parse Standard` fallback 이 안정적으로 동작했다.
- `25/26` 조합은 기존 HWP 추출기와 PDF 기준문 추출기가 안정적으로 동작했다.
- 현재 `LLM_MODE=mock` 이므로, 이번 결과는 법률 해석 품질 비교가 아니라 추출 품질과 파이프라인 일관성 검증으로 해석해야 한다.
- 실제 조문 차이 해석 품질을 보려면 다음 단계에서 live LLM 모드로 같은 시나리오를 다시 실행해야 한다.

## Artifacts

- JSON 상세 결과: `evaluation/dryrun-24-25-26-quality-20260402.json`
- OCR 단건 확인: `evaluation/upstage-ocr-check-20260402.json`
- 서버 로그: `evaluation/local-run/api.log`, `evaluation/local-run/web.log`
